# calendar2workours

Converts a isc calendar file into a csv file.

```bash
python -m venv env
source env/bin/activate
pip install icalendar
```

## Usage
```bash
python main.py in.ics -o out.csv
```
